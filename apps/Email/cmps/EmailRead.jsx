export function EmailRead({ emailsRead, emails }) {
    return (
        <section>
            <h3>
                {emailsRead} Emails read out of {emails}
            </h3>
        </section>
    );
}
